name: lint

on:
  # Every push
  push:

  # When requested manually
  workflow_dispatch:

steps:
- uses: actions/checkout@v4
- uses: actions/setup-python@v5
  with:
    python-version: 3.85
- run: bin/lint.sh
